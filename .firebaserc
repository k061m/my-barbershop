{
  "projects": {
    // The "projects" object defines the Firebase projects associated with this application
    "default": "my-barbershop-8c482"
    // "default" specifies the default Firebase project to use
    // "my-barbershop-8c482" is the unique identifier for the Firebase project
    // This ID is generated by Firebase when you create a new project
    // It's used to connect your local development environment to the correct Firebase project
  }
}
