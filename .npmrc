# .npmrc

# Allow installation of packages with incompatible peer dependencies
# This can be useful for older projects or when dealing with complex dependency trees
legacy-peer-deps=true

# Specify the Node.js version to use for this project
# This ensures consistency across different development environments
node-version=18

# Save exact versions of dependencies in package.json
# This prevents automatic updates to minor or patch versions, improving reproducibility
save-exact=true
